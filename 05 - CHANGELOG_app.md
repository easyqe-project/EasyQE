# Changelog — EasyQE (già «App Gestione Quadro Economico», rinominata l'11-7-2026)

## v1.00 — 13-8-2026 (`EasyQE.html`) — **prima versione di produzione**

- **Novità (14-8-2026, richiesta da Mauro): avviso di chiusura finestra/scheda con lavoro non salvato.** Introdotto tracciamento dello stato "modifiche non salvate" (`modificheNonSalvate`, nuovo flag globale) e un listener `beforeunload` che attiva il dialogo nativo di conferma del browser se il flag è vero al momento della chiusura. **Chiarito con Mauro prima di implementare**: il testo del dialogo non è personalizzabile (i browser principali, dal 2016-2017, ignorano qualunque messaggio passato a `beforeunload` e mostrano un proprio avviso generico fisso) — accettato esplicitamente, si attiva/disattiva solo il dialogo nativo, non il suo contenuto. Il flag diventa vero in `appendEvento()` (unico punto di scrittura su `db.registro_eventi`, quindi copre eventi "veri" da Registro eventi, nomine, cessazioni, rettifiche e chiusure di fase — verificato leggendo tutti i chiamanti prima di scrivere il codice) e in `salvaBozza()` (bozza in memoria, non ancora su disco) — **su richiesta esplicita di Mauro, una bozza in sospeso conta come lavoro non salvato**. Torna falso dopo un `salvaFile()` riuscito (entrambi i percorsi: `showSaveFilePicker` e il fallback `<a download>`), dopo l'apertura di un file (`apriFile()`) e alla creazione di una nuova opera (`nuovoFile()`). **Non coperto, segnalato esplicitamente come confine**: modifiche ai campi di "Dati generali di base" digitate ma non ancora passate in `db` tramite `leggiTestata()` (avviene al cambio scheda) — non sono ancora nel modello dati, quindi il meccanismo non le vede. Verificato: `node --check` sullo script estratto (nessun errore di sintassi); nessuna nuova occorrenza di `fetch`/`XMLHttpRequest`/`sendBeacon`/`WebSocket`/`localStorage`/`sessionStorage`/`indexedDB`/`document.cookie` (file resta offline e autocontenuto); non verificabile in UI in questa sessione per assenza di browser nel sandbox — **da collaudare in UI da Mauro** (chiusura scheda con evento registrato/bozza in sospeso non salvati → dialogo atteso; chiusura dopo «Salva» riuscito → nessun dialogo). Nessuna modifica a `nomeFile()`, allo schema JSON, agli export CSV o a logica di calcolo/validazione. Versione mantenuta v1.00 su indicazione esplicita di Mauro (non si apre una nuova revisione per questa aggiunta); `_versioni di lavoro/EasyQE_v1.00.html` riallineato al contenuto corrente.
- **Documentazione (14-8-2026, richiesta da Mauro): completata la correzione delle date 16-7-2026 → 13-8-2026 nei quattro punti residui e riqualificata v0.12 come release a sé.** Date corrette: voce sul manuale d'uso (sezione v0.11 qui sotto), doc 00 "Release corrente" (due occorrenze), doc 00 nota su v0.11, doc 00 titolo "Manuale d'uso". v0.12: eliminate le formule "mai stata una release a sé" in doc 00 e nella voce della decisione di produzione qui sotto — v0.12 è una release a sé, archiviata in `_versioni di lavoro/EasyQE_v0.12.html`, contenuto identico a v1.00 salvo un commento JS. Conseguenza recepita nella nota su v0.11 in doc 00: non è più "ultima release della linea 0.xx" (lo è v0.12). Non toccati: la struttura delle sezioni di questo changelog (le tre modifiche del 13-8 restano elencate sotto v1.00, senza una sezione v0.12 dedicata) e il riferimento del doc 00 alla sessione template xlsx del 16/07/2026 (data reale). Nessuna modifica al codice dell'app.
- **Documentazione (14-8-2026, segnalazione di Mauro): corrette in questo changelog le date della release v1.00 — da 16-7-2026 a 13-8-2026.** La decisione di congelare la build di produzione v1.00 e le tre modifiche assorbite (titolo di stampa, rimozione della versione dall'intestazione, rinomina scheda) sono del 13-8-2026, non del 16-7-2026 come erroneamente registrato. Riscontro sui metadati dei file: `_versioni di lavoro/EasyQE_v0.12.html` 13-8-2026 11:44, `_versioni di lavoro/EasyQE_v1.00.html` ed `EasyQE.html` di root 13-8-2026 11:49. Corretti il titolo della sezione v1.00 e le date delle tre voci. **Non toccati, in attesa di conferma di Mauro** (stesso pattern di errore, perimetro non incluso nella segnalazione): la voce sul manuale d'uso in sezione v0.11 (datata 16-7-2026; il file `EasyQE - Manuale d'uso.docx` risulta del 13-8-2026 11:58) e i passaggi corrispondenti del doc 00 ("Release corrente", nota su v0.11 "superata da v1.00 il 16-7-2026", titolo sezione "Manuale d'uso (nuovo, 16-7-2026)"). Il riferimento del doc 00 alla sessione template xlsx del 16/07/2026 è invece corretto e non va toccato: i file template portano date reali del 16-7-2026. Nessuna modifica al codice dell'app.
- **Documentazione (14-8-2026, approvata da Mauro): aggiunta in doc 00 la voce "App standalone compilata" alle voci valutate e scartate — chiude definitivamente il tema sospeso poche ore prima (voce precedente qui sotto).** Valutate e scartate tutte le strade (launcher Chrome `--app`, wrapper Electron, guscio Tauri, riscrittura compilata): firma/notarizzazione a pagamento indispensabile per il "scarica e lancia" di un utente qualunque, "file unico" comunque impossibile su macOS, vantaggio residuo solo estetico — `EasyQE.html` resta l'unica forma che soddisfa file unico + cross-platform + zero spese + zero installazioni. Registrata come idea aperta a costo zero, su richiesta di Mauro ("sì concettualmente, non ora"): icona personalizzata da assegnare al file HTML via Finder, lancio dal lato documenti del Dock; icona non prodotta per sua scelta. Contestualmente, titolo della sezione del doc 00 alleggerito dalla data (spostata sulle singole voci) per accogliere scarti successivi al 15-7-2026. Nessuna modifica al codice dell'app.
- **Documentazione (14-8-2026, approvata da Mauro): corretta in doc 00, sezione "Fonti di verità", la segnalazione di assenza di `QE - il libro.pdf`.** Il file è di nuovo presente nella cartella di progetto: reinserito da Mauro il 13-8-2026 alle 16:40 (poche ore dopo la stesura della segnalazione, stesso giorno), conferma esplicita di Mauro il 14-8-2026, presenza verificata nell'elenco file. La voce del 13-8-2026 qui sotto resta come traccia storica: al momento in cui fu scritta l'assenza era reale. Verifiche dottrinali di nuovo possibili. Nessuna modifica al codice dell'app. Nella stessa sessione, valutata su richiesta di Mauro un'app standalone (wrapper Electron o launcher Chrome `--app` per Dock e icona propria): analisi presentata, decisione di Mauro di sospendere il tema per ora — nessun file creato.
- **Documentazione (13-8-2026, richiesta da Mauro, in vista della chiusura del progetto e apertura di uno nuovo): trascritte in `00 - STATO PROGETTO...md` informazioni finora presenti solo nella memoria di sessione, che un nuovo progetto non erediterebbe.** (1) Corretti tutti i riferimenti a `versioni/` in `_versioni di lavoro/` (nome reale della cartella d'archivio, mai coincidente col nome usato nel testo). (2) Sezione "Fonti di verità" aggiornata: elencati anche i quattro allegati normativi di supporto oltre ad `art_5_allegato_I-7.pdf`; **segnalata l'assenza di `QE - il libro.pdf` dalla cartella di progetto** (verificato il 13-8-2026, cercato per nome in root e sottocartelle, non trovato) — fonte dottrinale primaria citata per paragrafo in doc 00-03 e changelog, da ritrovare prima di ogni futura verifica dottrinale; aggiunta la gerarchia di autorevolezza tra doc 01→02→03→04 (prima solo in memoria). (3) Nuova sezione "Pattern di lavoro validati su questo progetto": cinque episodi concreti (verifica dottrinale prima di obiettare, fermarsi al perimetro letterale dichiarandolo esplicitamente, cercare pattern-gemelli dopo un bug "primo risultato", riverificare tutte le letture di un campo dopo un cambio di tipo, distinguere un'informazione nuova da una semplice insistenza) trascritti dalla memoria di sessione. Nessuna modifica al codice dell'app.
- **Documentazione (13-8-2026, richiesta da Mauro): recepiti in doc 02 gli ultimi due rilievi ancora validi del doc 06 (report reverse engineering v0.06), svuotandolo di contenuto residuo utile.** (1) §4, riga `tipo_innesco`: l'enum dichiarato `atto | sal | contabile` non corrispondeva al codice — `sal`/`contabile` non sono mai stati implementati, gli unici valori usati sono `atto` (eventi di sistema) ed `evento` (aggiornamenti QE da `registraEvento()`); corretto con strikethrough + nota datata, stile già in uso nei documenti. (2) §10 "Catalogo eventi (consolidato)": verificato che **nessuno** dei ~20 tipi elencati (`ATTIVAZIONE_VOCE`, `IMPEGNO`, `LIQUIDAZIONE`, `SAL`, `COSTITUZIONE/VINCOLO/UTILIZZO/RESTITUZIONE_ECONOMIE`, `ACCORDO_BONARIO`, `TRANSAZIONE`, `COLLAUDO`, `AGGIUDICAZIONE`, ecc.) esiste più nel codice — il catalogo reale (`EVENTI_LBL`/`eventiAmmessi()`: `STIMA_INIZIALE`, `AFFINAMENTO_STIMA`, `CONSOLIDAMENTO_QE`, `REVISIONE_POST_GARA`, `MODIFICA_QE`, `REVISIONE_PREZZI`, `VARIANTE_120`, `RICONCILIAZIONE_QE`, `NOMINA`, `CESSAZIONE_INCARICO`, `RETTIFICA`, `CHIUSURA_FASE`) è un'evoluzione post-v0.06 mai riportata in doc 02. Sostituito il paragrafo con una nota di supersessione datata che rimanda al codice come fonte di verità, senza duplicare un catalogo da tenere sincronizzato a mano. Nessuna modifica al codice dell'app. Con questi due recepimenti, `06 - REPORT - Reverse engineering EasyQE_v0.06 vs doc 01-04.md` non contiene più informazioni non recepite altrove — decisione su archiviazione lasciata a Mauro.
- **Convenzione di progetto (13-8-2026, richiesta da Mauro): i documenti di progetto (modello dati, specifiche, stato, changelog — non i file dell'app, non gli allegati normativi, non i template) portano da ora un prefisso numerico a due cifre nel nome, per rendere leggibile il flusso logico-temporale dal solo elenco file.** Stato attuale, già conforme: `00 - STATO PROGETTO...md` … `06 - REPORT - Reverse engineering EasyQE_v0.06 vs doc 01-04.md` (quest'ultimo rinominato da Mauro, era `CHANGELOG_app.md` senza prefisso fino a diventare `05 - CHANGELOG_app.md`). Nessuna modifica al codice dell'app, nessun congelamento di versione richiesto. Nota aggiunta in `00 - STATO PROGETTO...md`; prossimo documento di questo genere riprende da 07.
- **Decisione di Mauro: questa build è la prima versione di produzione, numerata 1.00 (non più 0.xx) e distribuita come `EasyQE.html`, senza numero di versione nel nome del file.** Le tre modifiche del giorno (titolo di stampa, rimozione del numero di versione dall'intestazione, rinomina scheda) erano state rilasciate come **v0.12, release a sé** (archiviata in `_versioni di lavoro/EasyQE_v0.12.html`); v1.00 ne eredita integralmente il contenuto. **Convenzione di ora in poi**: il file di produzione (`EasyQE.html`) non porta il numero di versione nel nome; a ogni nuova versione congelata, la copia storica va comunque archiviata in `versioni/` con il nome versionato (`EasyQE_v1.0X.html`) — la regola ferrea di non sovrascrivere mai una versione precedente resta invariata, cambia solo il nome del file "corrente". L'unico riferimento interno al numero "v0.12" nel codice (un commento JS che documentava la correzione del titolo di stampa) è stato aggiornato a "v1.00" per coerenza; i commenti storici che citano "v0.11" e versioni precedenti restano invariati, sono cronaca di quando quelle funzionalità furono introdotte e non vanno rietichettati. `EasyQE_v0.12.html` (il file "di lavoro" di oggi, release a sé) rimosso dalla root su richiesta di Mauro, previa autorizzazione esplicita alla cancellazione (i file di progetto non si eliminano senza conferma); `versioni/EasyQE_v1.00.html` creato come archivio della release. `versioni/EasyQE_v0.12.html`, creato per pochi minuti nella stessa sessione, lasciato come è (mai sovrascritto/cancellato, per la stessa regola ferrea) — contenuto identico a v1.00 salvo quel singolo commento.
- **Novità (13-8-2026, richiesta da Mauro): scheda "Testata e fonti" rinominata in "Dati generali di base".** Solo l'etichetta in `TABS` (riga 511): chiave interna `testata`, `id`/nomi di funzione (`rTestata()`, `leggiTestata()`, ecc.), schema dati (`db.testata`) tutti invariati — nessun impatto su salvataggio, apertura file o logica. **Aggiornati anche, su conferma esplicita di Mauro**, i 3 messaggi dell'app che citavano il vecchio nome per indirizzare l'utente alla sezione: il tooltip di e.17 quando l'opera non è soggetta alla L. 717/1949, l'alert dello stesso caso in `calcolaL717()`, e l'avviso mostrato quando il primo evento di fase F.01 non può concludersi perché "Tipologia dell'intervento"/"Soggetta L. 717/1949" non sono ancora valorizzati. Nessuna occorrenza residua di "Testata e fonti" nel file.
- **Correzione (13-8-2026, richiesta da Mauro, stesso giorno): tolto il numero di versione dall'intestazione a schermo dell'app.** `<h1>EasyQE <span>v0.11</span></h1>` (riga 67) diventa `<h1>EasyQE</h1>` — il nome resta, il numero di versione no. Ripensamento rispetto alla voce precedente di changelog (quella riguardava il titolo di stampa, dove l'h1 non compare comunque, e lì Mauro aveva confermato di lasciarlo); qui la richiesta è specifica alla visualizzazione a schermo. Nessun'altra occorrenza del numero di versione resta visibile in UI o in stampa: le uniche "v0.1x" rimaste nel file sono commenti JS storici, mai renderizzati.
- **Correzione (13-8-2026, richiesta da Mauro con specifica tecnica dettagliata): il titolo di stampa non deve identificare lo strumento.** I browser riportano di default `document.title` nelle intestazioni/piè di pagina di stampa; poiché il `<title>` era "EasyQE — v0.11", ogni copertina stampata recava nome e versione dello strumento invece di un dato dell'opera. Corretto in tre punti, tutti gli unici toccati (diff di 3 blocchi, +12 righe, nessuna riga esistente rimossa né riformattata): (1) `<title>` sostituito con la stringa neutra "Quadro economico"; (2) `stampaCopertina()` imposta ora `document.title` alla denominazione dell'opera (`db.testata.denominazione`, con fallback su "Quadro economico" se vuota) subito prima di `window.print()`; (3) il titolo viene ripristinato dopo la stampa tramite l'evento `afterprint` (non una riga subito dopo `window.print()`, per evitare che in alcuni browser il ripristino avvenga prima che il motore di stampa legga il titolo), con una funzione dichiarata una volta sola (`ripristinaTitoloStampa()`) e riusata: `addEventListener` scarta automaticamente un listener duplicato con lo stesso riferimento di funzione, quindi resta registrato una sola volta anche richiamando `stampaCopertina()` più volte nella stessa sessione. Stringa di versione "v0.11" mantenuta in un commento JS (traccia interna, non nel DOM), su richiesta esplicita di Mauro. **Deliberatamente non toccato**: l'`<h1>EasyQE v0.11</h1>` dell'intestazione applicativa (riga 67) — non compare mai in stampa (`@media print` nasconde tutti i figli diretti di `body` tranne `#copertina`), quindi non necessario per il problema; segnalato a Mauro come decisione sua, non presa in autonomia — ha confermato di lasciarlo (poi rimosso comunque nella voce successiva, ma per un motivo diverso: la visualizzazione a schermo, non la stampa). Nessuna modifica a logica di calcolo/validazione/proiezione/serializzazione, a `nomeFile()`, allo schema JSON o agli export CSV, a commenti preesistenti. **Verificato**: nessuna occorrenza residua di "EasyQE" nel DOM o nell'output stampato (solo riga 67, UI a schermo mai stampata, e riga 1277, commento JS) — nessuna occorrenza di nomi di autori ("Moroni"/"Mazza") nell'HTML costruito da `stampaCopertina()` (compaiono solo nei tooltip dell'icona informativa del Registro eventi, mai stampati); confermata assenza di `fetch`/`XMLHttpRequest`/`sendBeacon`/`WebSocket`/script o link esterni/`@import`/`localStorage`/`sessionStorage`/`indexedDB`/`document.cookie` — file integralmente offline e autocontenuto, come prima.

## v0.11 — 14-7-2026 (`EasyQE_v0.11.html`) — **collaudo completo il 15-7-2026, release stabile**

- **Documentazione (13-8-2026, richiesta da Mauro): primo manuale d'uso di EasyQE, in formato .docx, costruito sul template aziendale "template_01.docx" caricato in progetto** (`EasyQE - Manuale d'uso.docx`). Non tocca il codice dell'app, non richiede congelamento di versione. Target concordato con Mauro (via intervista): un tecnico che conosce già il QE e sa compilarne le voci principali, ma non conosce le regole di controllo specifiche di questa app; profondità "completo"; schermate reali dell'applicazione; frontespizio a uso interno, nessun cliente. Undici capitoli più due appendici: introduzione (modello dati eventi/fasi/snapshot, catena hash), primo avvio e gestione del file, Testata e fonti, Soggetti, Registro eventi, un capitolo dedicato per intero alle regole di controllo (avvertenza vs blocco, R-01 su e.5 sempre bloccante dal primo evento più il tetto e.1+e.5, R-02, R-08, R-09, R-13, R-14, R-17, R-19 — quest'ultima raccolta fatta rileggendo il codice sorgente riga per riga, non a memoria, comprese regole come R-14/R-17 mai finora raccolte in un unico elenco), i calcoli automatici (e.17 con la formula a scaglioni fissi corretta ieri, Contributo ANAC), Snapshot e chiusura fase, Esporta, un glossario di tutte le voci del QE e un riepilogo tabellare di tutte le regole. Undici schermate reali catturate da un'istanza viva dell'app (non mockup): niente sandbox headless disponibile (Playwright bloccato da una libreria di sistema mancante, non installabile senza privilegi root; WeasyPrint scartato perché non supporta il CSS Grid usato dall'app e renderizza contenuto mancante) — soluzione adottata, con conferma di Mauro: server HTTP locale avviato da lui sul proprio Mac, pilotato via Chrome MCP. Cover, indice (TOC reale di Word, non testo statico — si aggiorna da sé aprendo il file) e struttura a capitoli/paragrafi tramite gli stili nativi del template (Titolo1/2/3, numerazione automatica); tabelle con lo stile "Grigliatabella" del template. Verificato: validazione XSD del docx, conversione in PDF e lettura visiva di tutte le 18 pagine generate, correggendo in un secondo passaggio un sottotitolo di copertina troppo lungo (andava a capo fuori dal riquadro) e l'intestazione di pagina ereditata dal template ("descrizione contrattuale del servizio", non pertinente a un manuale d'uso interno) sostituita con "Manuale d'uso — EasyQE". Nota per Mauro: aprendo il file in Word, indice e numeri di pagina si aggiornano da soli (sono campi nativi, non testo statico) — LibreOffice, usato solo per la verifica visiva offline, non li ricalcola in automatico.
- **Correzione (15-7-2026, da approfondimento giuridico di Mauro — tema chiuso): la formula di e.17 (L.717/1949) torna a scaglioni FISSI sull'INTERO importo, non progressivi/marginali.** Terza correzione della stessa formula nello stesso giorno. Riepilogo dell'iter: (1) prima richiesta con interpolazione lineare — segnalata la discrepanza col testo di legge (scaglioni fissi, nessuna interpolazione); (2) Mauro ha obiettato che gli scaglioni fissi produrrebbero un risultato non monotono (un'opera da 15M pagherebbe più di una da 25M — "una sciocchezza") e ha chiesto uno schema a scaglioni PROGRESSIVI MARGINALI (come le aliquote IRPEF), implementato e verificato sul suo esempio (30.000.000→280.000€); (3) **Mauro ha poi approfondito con fonti giuridiche** (non specificate in dettaglio) e ha confermato che la regola corretta è quella del testo letterale dell'art. 1 della legge (già individuato e citato per esteso da questa stessa sessione, §3.2.16 del libro, p. 67): l'aliquota di fascia si applica all'INTERO importo lavori, non alla quota che eccede la soglia inferiore — 0€ fino a 1.000.000€, poi 2%/1%/0,5% sull'intero importo a seconda della fascia (1-5M / 5-20M / oltre 20M). Esempio dato da Mauro: 1.100.000€ → 22.000€ (non 2.000€ come con lo schema progressivo del punto 2, ora sostituito). **Segnalato prima di implementare** (non un'obiezione, una verifica di coerenza): questa regola riproduce esattamente il notch di non-monotonia del punto (2) — 25.000.000€ (125.000€) paga meno di 15.000.000€ (150.000€); ulteriori notch anche alle soglie di 5M e 20M (il calcolo quasi dimezza appena sopra la soglia, es. 20.000.000€→200.000€ ma 20.100.000€→100.500€). Mauro ha confermato di essere consapevole e ha chiuso il tema. Modificati `L717_SCAGLIONI` (da intervalli con quota marginale a soglie con aliquota piena) e `accantonamentoL717()` (ora un semplice lookup: aliquota della fascia individuata × importo intero, non più una somma di contributi di fascia). Nessun'altra funzione toccata: `calcolaL717()` continua a leggere la stessa base (lordo di gara se disponibile, altrimenti valore corrente di "a"). Verificato con 27 test Node (i 24 della versione precedente, con i valori numerici ricalcolati per il nuovo schema, più 3 nuovi: l'esempio 1.100.000→22.000 e i due notch aggiuntivi a 5M/20M documentati esplicitamente come comportamento atteso). Nessuna regressione sulle sette suite storiche di sessione (138 test totali, 0 KO). Appendice R del piano di collaudo riscritta con i nuovi valori attesi e un nuovo item R9 che documenta esplicitamente i notch.
- **Novità (15-7-2026, richiesta da Mauro): l'imprevisto e.5 sotto il 5% dei lavori a base di gara (R-01) blocca ora la registrazione di OGNI evento, non solo la chiusura di fase.** Mauro ha confermato che R-01 blocchi la chiusura fase (comportamento già esistente, invariato) e ha aggiunto un punto più stringente: gli imprevisti non vanno mai trascurati, nemmeno nella primissima stima del QE. In `valida()`, la condizione `e5<0.05*base` (una delle quattro regole della funzione, l'unica toccata) non passa più per il flag `contestoSnapshot`: va sempre in blocco (`B`), sia dentro `registraEvento()` (prima: solo avviso confermabile via `confirm()`) sia in `chiudiFase()` (già bloccante, invariato). **Deliberatamente NON estesa la stessa modifica** alla seconda condizione di R-01 (eccesso e.1+e.5 oltre il 10%), a R-02 (voce d), R-08 (e.9) o R-09 (e.10): Mauro ha parlato specificamente di imprevisti, non di un inasprimento generale delle validazioni — restano avvisi sul singolo evento, bloccanti solo a chiusura fase, come prima. Aggiornato anche il testo esplicativo sotto il pulsante "Chiudi fase e congela snapshot", che descriveva genericamente "R-01/R-02/R-08/R-09 diventano bloccanti (sui singoli eventi sono avvertenze)" — non più esatto per questa condizione specifica, ora distinta nel testo. Verificato con 8 test Node dedicati (blocco al primo evento con e.5 insufficiente; confine esatto al 5%, non bloccante; di poco sotto, bloccante comunque; l'altra metà di R-01 resta avviso; R-08 resta avviso; base=0 — nessun a/b ancora imputato — nessun controllo possibile; chiusura fase invariata) e l'intera suite storica di sessione rieseguita senza modifiche (135 test totali su 8 file, 0 KO). Aggiunta appendice U al piano di collaudo (U1-U7).
- **Decisione di roadmap (15-7-2026, richiesta da Mauro): "PDF generato dall'app" scartato.** Il PDF attuale degli snapshot (generato via stampa del browser) va bene così — non si costruisce un generatore PDF nativo. Nessuna modifica al codice. "Export xlsx su template di Mauro" resta invariato in roadmap, come cosa da fare in futuro.
- **Novità (15-7-2026, richiesta da Mauro): calcolo automatico proposto per e.18.7 (Contributo ANAC, §3.2.17.7 del libro), a scaglioni FISSI sull'"IMPORTO TOTALE DELL'APPALTO".** Tabella fornita da Mauro (Delibera ANAC 524 del 22-12-2025, confermata identica sul libro): esente sotto 40.000 €, poi un importo fisso per fascia (35/250/250/410/410/660/880/880 €) fino a 20.000.000 € e oltre — **a differenza della L.717/1949, qui NON c'è progressione**: la delibera stessa esprime un importo unico per fascia, applicato all'intero valore, non alla quota marginale. Nuove `ANAC_SCAGLIONI`/`contributoAnac(importo)` (semplice lookup) e `calcolaAnac()`. **Base di calcolo, su indicazione esplicita di Mauro**: non l'importo lavori "a" (come per la L.717/1949), ma "IMPORTO TOTALE DELL'APPALTO" (a+b+c+d, +a.1/a.2 se appalto integrato) — che l'utente deve poter aggiornare liberamente fino alla chiusura di **F.03/PFTE nell'appalto integrato** o **F.04/PE nell'appalto ordinario**, per poi congelarlo. Le due fasi coincidono numericamente (F.03 e F.04) ma sono fasi diverse nelle due sequenze, perché appalto integrato scambia PE/AGG rispetto all'ordinario: F.03 è sempre PFTE in entrambe le sequenze, F.04 è PE nell'ordinario ma AGG nell'integrato — da qui la necessità di due trigger espliciti diversi (`"PFTE"` per l'integrato, `"PE"` per l'ordinario), non un semplice indice di fase. Implementato: nuovo `ev.base_anac` allegato da `chiudiFase()` solo quando la fase appena chiusa coincide col trigger pertinente alla modalità di affidamento corrente; `proiezione()` lo raccoglie in un nuovo campo scalare `baseAnac`, con lo stesso principio "congelato al primo evento che lo porta, mai più sovrascritto" già usato per `baseGara` (ribasso di gara, v. voce precedente), ma con un trigger diverso (chiusura fase, non aggiudicazione) e senza essere per-voce (un solo importo, non un oggetto con più chiavi). `calcolaAnac()` usa `PCUR.baseAnac` se già congelato, altrimenti il totale corrente (REFCUR). Nuovo pulsante "Calcola (Contributo ANAC)" in tabella movimenti, **sempre visibile** (a differenza del pulsante di e.17, non dipende da alcuna classificazione preliminare dell'opera). Valore sempre modificabile a mano, come ECO/e.17 — utile anche per le esenzioni che il calcolo non può riconoscere dal solo importo (ricostruzione post-sisma 2016/2017, affidamenti ex D.M. MAECI 192/2017, entrambe citate dal libro). Verificato con 29 test Node dedicati: tutte le fasce e i relativi bordi esatti; calcolo sul totale corrente prima di qualunque congelamento; congelamento corretto su chiusura F.03/PFTE (integrato) e mancato congelamento sulla stessa fase nell'ordinario (dove il trigger è invece F.04/PE); congelamento su F.04/PE (ordinario); valore congelato che prevale sul totale corrente anche quando quest'ultimo cadrebbe in una fascia diversa (verifica esplicita, non solo casi in cui coincidono per coincidenza numerica); presenza del pulsante nell'HTML della tabella movimenti. Nessuna regressione sulle cinque suite precedenti (98 test). Aggiunta appendice T al piano di collaudo (T1-T8).
- **Novità (15-7-2026, richiesta da Mauro): nuovo campo "Tipologia dell'intervento" in Testata, che determina automaticamente "Soggetta L. 717/1949".** Select con quattro opzioni — edifici residenziali pubblici, edifici industriali, infrastrutture, altri edifici pubblici (nuova costante `TIPOLOGIA_INTERVENTO`) — coerenti con le esclusioni citate dal libro (§3.2.16): solo "altri edifici pubblici" resta soggetto all'obbligo. Selezionandola, `aggiornaSoggettaL717Auto()` scrive automaticamente "Sì" (se altri edifici pubblici) o "No" (per le altre tre) nel campo "Soggetta L. 717/1949" — che resta comunque un select modificabile a mano in qualunque momento, come ogni altro valore proposto nell'app (ECO, e.17). **Cambio di schema dati**: `testata.soggetta_L717` passa da booleano a stringa a tre stati (`""`=non ancora deciso/"--", `"si"`, `"no"`) per poter distinguere "non ancora scelto" da un "no" esplicito — entrambi i campi nascono vuoti su una nuova opera. Aggiunta migrazione difensiva in `apriFile()`: i file salvati con la vecchia versione (booleano) vengono convertiti in lettura, nessun cambio di `schema_version` (stessa tolleranza già usata per `bozza_evento` mancante). **Corretto un mio errore introdotto nella prima stesura**: `calcolaL717()` e la visibilità del pulsante "Calcola (L. 717/1949)" controllavano `if(!db.testata.soggetta_L717)`, che con una stringa `"no"` (truthy in JavaScript) avrebbe attivato il calcolo anche per opere esplicitamente NON soggette — corretto in `db.testata.soggetta_L717!=="si"` prima di eseguire alcun test.
  - **Novità collegata, stesso giorno: al salvataggio del primo evento "vero" della fase F.01/TRI, se "Tipologia dell'intervento" o "Soggetta L. 717/1949" non sono ancora valorizzati, l'evento non si perde.** `registraEvento()` controlla, prima di ogni altra validazione, se si tratta del primo evento con `tipo_innesco==="evento"` in fase TRI (le nomine/cessazioni di soggetti, `tipo_innesco:"atto"`, non contano ai fini di questo conteggio: possono precedere il primo evento "vero" senza che l'utente abbia ancora completato la Testata). Se la Testata non è completa: invece di un semplice blocco con messaggio, viene automaticamente salvata la bozza dell'evento in corso (`salvaBozza()`, equivalente a premere "Salva e continua dopo") e mostrato un invito a tornare in Testata, completare la classificazione, e tornare al Registro eventi per concludere — i dati già digitati non vanno persi, sfruttando il meccanismo di bozza già esistente.
  - Verificato con 24 test Node (formula L.717 invariata + nuovo automatismo + migrazione retrocompatibile) e 9 test dedicati al blocco sul primo evento (invocando `registraEvento()` per intero, non solo in isolamento: primo evento bloccato e bozza salvata, nomine pregresse che non "contano" come primo evento, registrazione regolare a Testata completa, nessun blocco fuori da F.01, caso limite con un solo dei due campi valorizzato). Nessuna regressione sulle quattro suite precedenti (65 test). Aggiunta appendice S al piano di collaudo (S1-S9).
- **Novità (15-7-2026, richiesta da Mauro): calcolo automatico proposto per e.17 (Opere artistiche L. 717/1949, §3.2.16 del libro), a scaglioni progressivi sull'importo lavori a base di gara.** Nuovo pulsante "Calcola (L. 717/1949)" accanto alla voce e.17 in tabella movimenti, visibile solo se `testata.soggetta_L717===true` (campo anagrafico già esistente, prima puramente informativo); se non soggetta, nota informativa e nessun calcolo, e.17 resta manuale come oggi. **Iter della richiesta, in tre correzioni successive dello stesso giorno**: (1) prima richiesta, con "interpolazione lineare" nei valori intermedi; verificato sul libro (§3.2.16, testo dell'art. 1 della legge riportato per esteso) che la norma prevede in realtà scaglioni fissi ad aliquota unica sull'intero importo, senza interpolazione — segnalata la discrepanza invece di implementare direttamente; (2) Mauro ha argomentato che gli scaglioni fissi produrrebbero un risultato non monotono (un'opera da 15M accantonerebbe più di una da 25M) e ha chiuso la discussione chiedendo di procedere con l'interpolazione; (3) prima di implementare l'interpolazione della percentuale, Mauro ha corretto la formula stessa: non un'aliquota interpolata sull'intero importo, ma uno **schema a scaglioni progressivi marginali** (come le aliquote IRPEF) — 0% sul primo milione, 2% sulla quota tra 1 e 5 milioni, un ulteriore 1% sulla quota tra 5 e 20 milioni, un ulteriore 0,5% sulla quota oltre 20 milioni. Esempio di verifica fornito da Mauro: 30.000.000 € → 280.000 € (0 + 80.000 + 150.000 + 50.000), ricalcolato indipendentemente prima di scrivere codice. Questo schema elimina il problema di non-monotonia segnalato al punto (2) per costruzione (ogni fascia aggiuntiva aggiunge sempre un contributo ≥ 0). Implementato: nuove `L717_SCAGLIONI` (tabella soglie/aliquote) e `accantonamentoL717(importo)` (somma dei contributi di fascia); `calcolaL717()` legge l'importo lavori "a" — se l'aggiudicazione è già avvenuta usa la base di gara congelata (lordo, stesso meccanismo di R-01/R-02/R-08/R-09), altrimenti il valore corrente di "a" nella fase (ricalcolato a ogni evento finché non c'è aggiudicazione, su indicazione di Mauro) — e scrive il risultato in e.17, sempre modificabile a mano come ECO. Verificato con 16 test Node dedicati: formula esatta sull'esempio di Mauro (30M→280.000) e su altri importi, monotonia, continuità ai nodi di soglia (1M/5M/20M), azzeramento sotto il primo milione, alert quando l'opera non è soggetta, uso corretto di REFCUR prima dell'aggiudicazione vs. base di gara dopo. Nessuna regressione sulle quattro suite precedenti (10+15+17+23=65 test, tutti ancora passati). Aggiunta appendice R al piano di collaudo (R1-R8).
- **Decisione di roadmap (15-7-2026, richiesta da Mauro): "IVA automatica per voce" scartata.** La casistica IVA è troppo varia e spesso dubbia, richiede sempre una valutazione professionale individuale — l'IVA (e.18 e sotto-voci, §3.2.17 del libro) resta imputabile solo manualmente, come già oggi. Nessuna modifica al codice; rimossa dalla roadmap dichiarata in STATO PROGETTO, spostata tra le voci scartate.
- **Collaudo (15-7-2026, eseguito da Mauro in UI): appendici O, P e Q tutti passati** — chiude il debito di collaudo residuo delle tre correzioni/novità di giornata (selezione progettista in Registro eventi, nuove figure RP progettazione ed esecuzione/RP affidamento, elenco progettisti in copertina). Con questi, **l'intero piano di collaudo risulta passato su tutta la linea v0.06→v0.11** (test 1-24 + appendici A-Q), nessun debito residuo.
- **Correzione (15-7-2026, segnalata da Mauro): in copertina, "Identificazione figure chiave" mostrava un solo progettista invece di tutti quelli in carica.** Stesso bug di fondo già corretto in Registro eventi (appendice O): `inCarica()`, in `stampaCopertina()`, usava `db.soggetti.find(sg=>sg.ruolo===r&&!sg.cessazione)` — il primo risultato — per ogni ruolo indistintamente. Corretto per progettista, senza toccare gli altri ruoli: `inCarica("progettista")` ora richiama `progettistiAttivi()` (tutti i non cessati) e li elenca uno per riga (`<br>`), con etichetta `labelProgettista()` — le stesse due funzioni già scritte e testate per il menù a tendina del Registro eventi, riusate qui senza duplicazione. **Confermato con Mauro (chiesto esplicitamente prima di implementare, in dubbio se l'elenco dovesse includere anche i progettisti cessati per completezza storica)**: solo i progettisti attualmente in carica, stessa popolazione già usata altrove — nessuna indicazione di cessazione da aggiungere. RUP/DEC/le altre figure a titolare singolo (incluse le due nuove RP progettazione ed esecuzione/RP affidamento, v. voce successiva) restano invariate: continuano a mostrare un solo nominativo. Verificato con 10 test Node dedicati, che invocano `stampaCopertina()` per intero (non solo la funzione isolata): elenco corretto con più progettisti su livelli diversi, etichetta con ragione sociale per le società, esclusione dei cessati, placeholder "non ancora affidato" quando nessuno è nominato, nessuna regressione col singolo progettista, nessuna regressione su RUP quando più persone si sono succedute nel ruolo nel tempo. Nessuna regressione sulle suite precedenti (15+17+23 = 55 test, tutti ancora passati). Aggiunta appendice Q al piano di collaudo (Q1-Q5).
- **Novità (15-7-2026, richiesta da Mauro): due nuove figure, "RP progettazione ed esecuzione" e "RP affidamento".** Mauro ha precisato, correggendo un dubbio sollevato subito dopo la correzione della selezione progettista (v. voce successiva): RUP e DEC **non** condividono il problema del "primo nominato" — è impossibile che due RUP o due DEC siano in carica contemporaneamente. Le due nuove figure richieste seguono lo stesso principio (una sola persona in carica alla volta), quindi trattate esattamente come RUP/DEC, non come il progettista. Aggiunte due voci a `RUOLI` (`rp_progesec`→"RP progettazione ed esecuzione", `rp_affidamento`→"RP affidamento") e due radio button in "Responsabile attività di fase" (Registro eventi), accanto a RUP/DEC/Progettista. **Nessun'altra modifica al codice**: `autofillResp()` (ramo non-progettista), `tipologieAmmesse()` (fallback generico `["persona_singola","societa"]`, le due nuove voci non sono in `RTI_AMMESSO`/`COMMISSIONE_AMMESSA`), `ruoloUI()` (il box "livello" resta condizionato a `r==="progettista"`, non tocca le nuove voci), `registraEvento()`/`raccogliBozza()` (leggono il valore del radio in modo generico) e la tabella "Identificazione figure chiave" in copertina (itera su `RUOLI`) già supportavano qualunque nuovo ruolo senza modifiche, essendo tutti scritti in modo generico sul valore del ruolo — verificato leggendo il codice riga per riga prima di scrivere, non per assunzione. Verificato con 15 test Node dedicati (stesso stub DOM con supporto radio usato per la correzione progettista): presenza ed etichette esatte in `RUOLI`; `tipologieAmmesse()` identica a RUP; autofill diretto sul campo di testo libero (non l'elenco a tendina del progettista) per entrambe le nuove figure; campo vuoto senza errori quando nessuno è in carica o l'unico nominato è cessato; box "livello" invariato. Nessuna regressione sulle suite precedenti (23 test ribasso + 17 test progettista, tutti ancora passati). Aggiunta appendice P al piano di collaudo (P1-P6) per il collaudo di Mauro in UI. **Effetto collaterale corretto di conseguenza**: la riga radio "Responsabile attività di fase" passava da 3 a 5 opzioni, due delle quali con etichette lunghe ("RP progettazione ed esecuzione") — il contenitore (`.radio-riga`, `display:flex` senza `flex-wrap`) avrebbe tagliato le opzioni fuori schermo su finestre non massimizzate. Aggiunto `flex-wrap:wrap` (solo CSS, nessuna logica toccata).
- **Correzione (15-7-2026, segnalata da Mauro): il responsabile evento "progettista" autocompilava sempre il primo progettista nominato, anche quando ne esistevano altri in carica su livelli diversi (DOCFAP/PFTE/PE nominati liberamente).** `autofillResp()` usava `db.soggetti.find(...)`, che restituisce sempre il primo risultato: corretto per gli altri ruoli (RUP/DEC, normalmente un solo soggetto in carica), ma sbagliato per "progettista", dove più soggetti non cessati sono la norma. Corretto: quando il responsabile scelto è "progettista", il campo "Nominativo" (libero) viene sostituito da un elenco a tendina (`eRedNProg`, nuovo box `eRedNProgBox`) con **tutti** i progettisti in carica (`progettistiAttivi()`, filtro `!cessazione`), etichettati col livello tra parentesi quando presente in anagrafe (es. "Mario Rossi (PFTE)") e con la ragione sociale se il soggetto è una società o un RTI (es. "Luca Verdi (PE — Studio Verdi Srl)"); di default resta selezionato il primo (comportamento di partenza invariato), ma ora è visibile e modificabile con un clic invece di un'autocompilazione silenziosa e indistinguibile. Il campo `eRedN` resta l'unico che viaggia nell'evento — **nessuna modifica a `registraEvento()`, `raccogliBozza()` o allo schema salvato**: il select si limita a scriverci dentro il nominativo scelto (`aggiornaRedNDaSelect()`). Per RUP/DEC il comportamento è **identico a prima** (campo di testo autocompilato con l'unico soggetto in carica). Gestita anche la ripresa di una bozza salvata con responsabile "progettista": il select si riallinea al nominativo effettivamente salvato, non al primo candidato di default. Verificato con 17 test Node dedicati (stub del DOM con supporto ai radio button): elenco corretto con più progettisti (persona singola e società), etichette con livello e ragione sociale, default sul primo mantenuto, cambio di selezione propagato a `eRedN`, nessuna regressione su RUP/DEC, ripresa bozza riallineata, elenco vuoto gestito senza errori (placeholder, non crash), progettisti cessati esclusi dall'elenco. Nessuna regressione sulla suite di test della correzione ribasso (23/23 ancora passati). Aggiunta appendice O al piano di collaudo (O1-O6) per il collaudo di Mauro in UI.
- **Collaudo (15-7-2026, eseguito da Mauro in UI): appendici C4, E5, L9 e N1-N8 tutti passati** — chiude il debito di collaudo residuo (C4 aperto dal 12-7-2026, E5 dal 13-7-2026, L9 e N introdotte questa settimana). Con questi, **l'intero piano di collaudo risulta passato su tutta la linea v0.06→v0.11** (test 1-24 + appendici A-N), nessun debito residuo. `EasyQE_v0.11.html` è a tutti gli effetti release stabile, come lo fu la v0.06 dopo i test 1-24.
- **Correzione (15-7-2026, segnalata da Mauro in collaudo): il ribasso di gara riduceva solo i totali aggregati, non le singole voci "a"/"d"/"a.1"/"a.2" — "i conti non tornavano".** In fase AGG, `calcolaEco()` calcolava ECO/ECO_ING sul totale ribassabile (a+d, a.1+a.2) ma lasciava le singole voci al lordo: sommando a mano le righe della tabella il risultato non coincideva col totale mostrato in calce. **Verificato sul libro prima di correggere** (§3.3, 3.3.1: "l'importo del quadro A viene riscritto al valore contrattuale... la differenza — l'economia da ribasso — riemerge tra le somme a disposizione del quadro B, come voce autonoma"): la dottrina prescrive esplicitamente la riscrittura della singola voce, non solo la deduzione dal totale. Corretto in tre parti:
  1. **`calcolaEco()` riscrive ora anche "a", "d" (e "a.1"/"a.2" in appalto integrato) al valore netto**, applicando la stessa percentuale usata per ECO direttamente a ciascuna voce (richiesto esplicitamente da Mauro: "si applica quella percentuale a ogni singola voce"), col tetto del 35% (art. 41 c.15-bis) che continua a valere solo per a.1/a.2, esattamente come già per ECO_ING.
  2. **Nuovo meccanismo "base di gara"**: dato che R-01/R-08/R-09 (soglie su e.1/e.5/e.9/e.10) e R-02 (voce d) devono restare ancorate al valore **lordo** — già confermato da Mauro il 14-7-2026 — introdotta una variabile transitoria `ULTIMO_RIBASSO_GARA` (valorizzata da `calcolaEco()` coi valori di REFCUR prima della riscrittura, sopravvive a "Salva e continua dopo" tramite `raccogliBozza()`/`db.bozza_evento.base_gara`) che `registraEvento()` allega all'evento come `ev.base_gara`. `proiezione()` la raccoglie in un nuovo campo `baseGara`, **congelato al primo evento che la porta e mai più sovrascritto** (coerente col libro: il ribasso "si compie una sola volta"). `valida()` usa `p.baseGara.a`/`p.baseGara.d` al posto del valore corrente per il calcolo di "base" (R-01/R-08/R-09) e per "d" in R-02; il "costo complessivo" di R-02 viene ricostruito al lordo come `totQE + eco + ecoIng` (relazione che vale per costruzione, essendo la stessa percentuale unica applicata sia all'aggregato sia alle singole voci).
  3. **Rimossa la sottrazione di ECO/ECO_ING dai totali** in `totali()`, `aggiornaTotali()` e `stampaCopertina()`: ora che lo sconto è già incorporato nelle singole voci, sottrarlo di nuovo a livello aggregato lo conterebbe due volte. ECO/ECO_ING restano visibili come riga a sé (tabella movimenti e copertina), a fini di trasparenza/tracciabilità (BDAP/MOP, art. 9/60/120/126 D.Lgs. 36/2023, libro §3.3), non più come deduzione.
  - **Compatibilità**: i file già usati per il collaudo di oggi con la vecchia logica (voci al lordo + sottrazione aggregata) mostreranno ora un doppio conteggio se riaperti con questa build — stesso trattamento già riservato in precedenza a cambi di meccanismo strutturale (v. nota v0.07 sulla migrazione del dizionario voci): da ricreare con "Nuova opera", non è prevista una migrazione automatica salvo richiesta.
  - Verificato con una suite di test Node dedicata (23 controlli, isolata dalle funzioni pure `proiezione()`/`totali()`/`valida()` e da `calcolaEco()` con uno stub minimale del DOM): riscrittura corretta di a/d (percentuale piena) e a.1/a.2 (percentuale con tetto 35%) in appalto integrato; nessuna regressione in appalto ordinario (campi a.1/a.2/ECO_ING assenti dal DOM, calcolaEco() li ignora correttamente); nessuna doppia sottrazione nel totale; R-08 che non scatta più per errore leggendo il netto invece del lordo; R-02 che ricostruisce correttamente il costo complessivo lordo; base di gara che resta congelata alla prima aggiudicazione anche dopo un evento successivo che modifica di nuovo "a"; base di gara che sopravvive al ciclo salva-bozza/riprendi. Aggiunta appendice N al piano di collaudo (N1-N8) per il collaudo di Mauro in UI.
- **Documentazione (15-7-2026, richiesta da Mauro): "PIANO DI COLLAUDO v0.07.md" convertito anche in PDF** (stesso nome base, `PIANO DI COLLAUDO v0.07.pdf`), con lo stesso contenuto integrale (tutte le appendici A-M) e due caselle di spunta OK/KO per ciascuna delle 66 voci di test, più una riga per annotare l'esito se KO. Nessuna modifica al testo normativo/di collaudo, solo formato — non tocca il codice dell'app, non richiede congelamento di versione. Il file .md resta la fonte, non sostituito.
- **Correzione (14-7-2026, segnalata da Mauro): le due voci di Quadro A per i servizi di ingegneria dell'appalto integrato rinominate da "e"/"f" a "a.1"/"a.2", e riposizionate subito sotto "MAN"** — Mauro ha confermato che la sovrapposizione con la lettera "e)" della norma (segnalata prima di scrivere codice) non era comunque una buona soluzione, anche accettandola consapevolmente. Aggiornati tutti i riferimenti nel codice: `vociBase()` (chiavi, posizione nell'array, `codice:"a"` come per le sotto-voci di e.8/e.9), `PARAGRAFO_LIBRO`, `RIB_KEYS_ING`, `calcolaEco()`, `stampaCopertina()`, tutte le etichette dei totali ("TOTALE SERVIZI DI INGEGNERIA SOGGETTO A RIBASSO (a.1 + a.2...)", "IMPORTO TOTALE DELL'APPALTO (...+ a.1 + a.2)"). Le due voci portano ora anche `sotto_voce_di:"a"`, ma **solo per l'indentazione visiva** in tabella movimenti e copertina: `voceVisibile()` verifica `solo_se_integrato_da_fase` prima di guardare `sotto_voce_di`, quindi non attiva alcuna logica di somma nella voce "a" — resta sempre e solo il valore dei lavori, senza includere i servizi di ingegneria. Verificato con test dedicato: ordine del dizionario (a, MAN, a.1, a.2, b, c, d); nessuna voce con chiave "e" o "f" residua; calcolo del doppio ribasso (percentuale unica + limite 35%) identico a prima, solo con le nuove chiavi; indentazione presente; "a" non inglobra a.1/a.2 nel proprio valore corrente.
- **Novità (14-7-2026, richiesta da Mauro): etichetta del campo "Modalità di affidamento" rinominata in "Tipologia di appalto"** nel form Testata e fonti (sezione "Dati sintetici salienti dell'opera"). Solo etichetta: id del campo (`tMod`), chiave dati (`testata.modalita_affidamento`) e valori dell'enum invariati.
- **Novità (14-7-2026, richiesta da Mauro): nuova riga "Tipologia di appalto" nella tabella "Sintesi e macro-dati" della copertina**, subito dopo la riga CIG — prima l'informazione non compariva da nessuna parte in stampa. Valore: "appalto ordinario" o "appalto integrato" (da `testata.modalita_affidamento`, sostituendo l'underscore con uno spazio). Verificato con test dedicato: etichetta corretta nel form, nessun residuo della vecchia etichetta; riga presente in copertina subito dopo CIG con il valore atteso.
- **Correzione (14-7-2026, segnalata da Mauro subito dopo la prima stesura della funzionalità appalto integrato): la percentuale di ribasso deve essere UNICA per lavori e servizi di ingegneria, non due percentuali indipendenti.** Rimosso il secondo campo percentuale (`mvPerc_ECO_ING`) e la funzione `calcolaEcoIng()` introdotti nella prima stesura; un solo input percentuale, sulla riga ECO, alimenta ora entrambe le economie tramite `calcolaEco()`. **Unico accorgimento, anch'esso richiesto esplicitamente**: sul solo calcolo dei servizi di ingegneria, se la percentuale supera il 35% viene comunque limitata al 35% (nuova costante `RIBASSO_ING_MAX=35`) — sui lavori la percentuale si applica invece per intero, senza limite. Trova fondamento nell'art. 41 c. 15-bis del D.Lgs. 36/2023 (limite al ribasso sulle spese tecniche nelle procedure OEPV), già individuato ma non implementato nella prima stesura di questa funzionalità. La riga ECO_ING in tabella movimenti non ha più un proprio input percentuale: mostra solo una nota "(stessa percentuale della voce ECO... limitata al 35%...)" e il valore calcolato, restando comunque modificabile a mano come ogni altra voce. Verificato con test dedicato: percentuale sotto il 35% (nessuna differenza tra le due basi); percentuale sopra il 35% (limite applicato solo ai servizi, i lavori restano al valore pieno); percentuale esattamente al 35%; cascata dei totali coerente in entrambi i casi; nessuna regressione sull'appalto ordinario (righe/campi ECO_ING mai renderizzati).
- **Appalto integrato, da fase PFTE: la progettazione esecutiva e il coordinamento sicurezza in fase di PE diventano servizi di ingegneria soggetti a ribasso, in Quadro A** (richiesta esplicita di Mauro, riscontrata nel libro "QE - il libro.pdf" §2.2/§3.3.2 e nell'art. 44 c.4 del Codice: l'offerta indica distintamente il corrispettivo per progettazione ed esecuzione). Tre effetti, tutti condizionati a `modalita_affidamento==="appalto_integrato"` e fase ≥ PFTE:
  1. Le sotto-voci `e.8.3` (Redazione del progetto esecutivo) ed `e.8.5` (Coordinamento sicurezza in fase di PE) escono da `e.8` e diventano due nuove voci di Quadro A, **`e`** e **`f`**, ribassabili. Danno origine a un nuovo totale **"TOTALE SERVIZI DI INGEGNERIA SOGGETTO A RIBASSO (e + f)"**, collocato subito sotto "TOTALE OPERE SOGGETTE A RIBASSO (a + d)", che si somma a sua volta a "IMPORTO TOTALE DELL'APPALTO" (ora "a + b + c + d + e + f" quando applicabile, altrimenti invariato "a + b + c + d").
  2. Le sotto-voci `e.9.3` ed `e.9.5` (stesse funzioni, versione personale interno) spariscono da `e.9`: in appalto integrato quelle funzioni non possono più essere svolte da personale interno (il libro, §3.2.7.3, è netto: "la corrispondente voce del QE... è semplicemente pari a zero"). Non sono spostate, semplicemente non contano più: R-08 (soglia 1,6% su e.9) somma automaticamente solo le sotto-voci rimaste, senza bisogno di toccare la regola.
  3. `ECO` si sdoppia: resta la riga esistente (ribasso su a+d) e se ne aggiunge una nuova, **`ECO_ING`**, per il ribasso sui servizi di ingegneria (e+f) — con una **percentuale indipendente**, non la stessa di ECO (l'offerta le indica separatamente). Entrambe sottratte alle rispettive basi, entrambe accantonate fuori dal totale QE B, entrambe mostrate dopo il TOTALE COMPLESSIVO.
  - **Naming delle nuove voci — punto sollevato e poi risolto con Mauro**: "e" ed "f" non sono lettere libere nell'art. 5 c.1 Allegato I.7 — la lettera "e)" della norma è già l'intera famiglia delle somme a disposizione (Quadro B, cioè tutto il blocco `e.1`…`e.18` di questa app) e non esiste una lettera "f)". Segnalato a Mauro prima di scrivere codice; **Mauro ha confermato di voler usare comunque "e" e "f"** per le due nuove voci di Quadro A, accettando la sovrapposizione visiva col Quadro B nella copertina stampata. Implementato esattamente così, nessuna nota o etichetta alternativa aggiunta.
  - **Meccanismo**: nuovo helper `voceVisibile(x,faseRifIdx)`, che generalizza il precedente `vociVisibili()` (finora basato solo su `sotto_voce_di`+`disaggAttiva`) con due nuovi attributi opzionali sulla voce: `solo_se_integrato_da_fase` (voce visibile SOLO in appalto integrato da quella fase in poi — usato da `e`, `f`, `ECO_ING`) e `escludi_se_integrato_da_fase` (voce normalmente visibile ma ESCLUSA in appalto integrato da quella fase — usato da `e.8.3`, `e.8.5`, `e.9.3`, `e.9.5`). `e.8.3`/`e.8.5`/`e.9.3`/`e.9.5` restano nel dizionario voci (non sono cancellate): per appalto ordinario si comportano esattamente come prima, senza alcuna modifica. Tutte le somme di sotto-voci (`proiezione()`, la cella calcolata in `aggiornaTotali()`) ora filtrano attraverso `voceVisibile()` prima di sommare, non solo `vociVisibili()` — altrimenti una sotto-voce nascosta con un valore storico residuo (es. dopo un cambio di modalità di affidamento a metà opera) sarebbe rimasta silenziosamente inclusa nel totale della voce madre.
  - **Nuova costante `RIB_KEYS_ING=["e","f"]`** (accanto a `RIB_KEYS=["a","d"]`), nuovo accumulatore `totIng` in `aggiornaTotali()`, nuova funzione `calcolaEcoIng()` gemella di `calcolaEco()` ma sulla base e+f. `totali()` sottrae ora sia `eco` sia `ecoIng` dal totale QE (nuovo campo `ecoIng` nel valore restituito). `valida()`: il calcolo di `totQE` esclude ora anche `ECO_ING` (come già faceva per `ECO`) — le voci `e`/`f` invece VI RIENTRANO regolarmente, sono un costo vero (non un'economia), esattamente come già accadeva per `e.8.3`/`e.8.5` quando facevano parte di `e.8`.
  - **Deliberatamente NON toccato**: R-01, R-02, R-08, R-09 continuano a leggere le basi originarie (`a`, `b`, `d`, `e.9`, `e.10` direttamente) — non referenziano mai il totale ribassabile in sé, quindi non risentono della nuova voce `e`/`f` se non nel modo già corretto e automatico descritto sopra per R-08. `csvProiezione()` e `csvRegistro()` non richiedono modifiche: iterano `vociVisibili()`/il registro eventi in modo generico, includono automaticamente le nuove voci.
  - **Trovato ma non implementato (fuori dal perimetro della richiesta)**: il libro (§2.2.2) segnala che il ribasso sulle spese tecniche in appalto integrato non è illimitato — vanno rispettati i limiti dell'art. 41 c. 15-bis del D.Lgs. 36/2023 per le procedure OEPV. Nessuna nuova regola di validazione aggiunta per questo limite: da valutare come eventuale voce di roadmap separata.
  - Verificato con test dedicato: voci visibili corrette per integrato@PFTE+ e per ordinario@PFTE+ (non-regressione); doppio ribasso con percentuali indipendenti (10% lavori, 25% servizi ingegneria) su un caso numerico completo, live e dopo salvataggio; cascata totRib+totRibIng+totNR=totApp; copertina con la nuova riga e l'etichetta corretta; nessuna regressione sulla suite di test delle versioni precedenti (v0.10: icona info, stima forfettaria, larghezze colonne, elenco pezze stratificato/cumulativo, ECO su a+d, messaggio di conferma).

## v0.10 — 13-7-2026 — **congelata il 14-7-2026, non si tocca più** (`EasyQE_v0.10.html`, archiviata in `versioni/`)

- **Novità (14-7-2026, richiesta da Mauro): messaggio di conferma dopo la registrazione riuscita di un evento.** Al termine di «Salva e concludi l'evento (snapshot)», se l'evento supera tutte le validazioni e viene effettivamente registrato, compare un messaggio di presa d'atto (`alert()`) con testo fisso: «L'evento è stato registrato correttamente e può essere visualizzato/stampato dalla sezione "snapshot e chiusura fase"». **Condizione applicata, non esplicitamente richiesta ma necessaria per non rendere il messaggio falso**: compare solo se l'evento genera effettivamente uno snapshot (`delta.length>0`) — l'unico evento che può registrarsi validamente senza movimenti è `RICONCILIAZIONE_QE` (riconciliazione senza scostamenti), che non genera snapshot: in quel caso il messaggio non compare, perché affermerebbe una cosa non vera. Non toccato il pulsante «Chiudi fase e congela snapshot» (`chiudiFase()`): la richiesta riguardava esplicitamente il pulsante di conclusione evento. Verificato con test dedicato: messaggio esatto mostrato dopo un evento con movimenti; nessun messaggio sui blocchi di validazione di base (campi obbligatori mancanti); nessun messaggio quando l'evento è bloccato da una regola (es. voce negativa); nessun messaggio per una riconciliazione senza scostamenti.
- **Novità (14-7-2026, richiesta da Mauro): l'economia da ribasso (ECO) viene ora sottratta dal "TOTALE OPERE SOGGETTE A RIBASSO".** Prima di questa modifica ECO era semplicemente esclusa dai totali (v. voce del 12-7-2026 più sotto); ora il suo valore assoluto (calcolato dalla percentuale di ribasso con `calcolaEco()` o imputato direttamente) viene **sottratto** da "TOTALE OPERE SOGGETTE A RIBASSO" (a+d), con effetto a cascata su "Importo totale dell'appalto" (a+b+c+d) e su "TOTALE COMPLESSIVO" — sia nella tabella movimenti (calcolo "in tempo reale" e valore precedente, `aggiornaTotali()`), sia in copertina (`stampaCopertina()`), sia nel campo `tot` restituito da `totali()`. Resta invariata l'esclusione di ECO dal Totale del Quadro B: ECO non è una voce di quadro B, compare come riga a sé dopo il TOTALE COMPLESSIVO, "accantonata". Etichetta della riga aggiornata in "TOTALE OPERE SOGGETTE A RIBASSO (a + d, al netto dell'economia da ribasso)" sia in tabella movimenti sia in copertina. **Scelta di perimetro confermata da Mauro (14-7-2026)**: le regole R-01, R-02, R-08, R-09 restano non modificate — continuano a leggere i totali al lordo dell'economia da ribasso, perché le soglie normative che verificano si riferiscono all'importo lavori/costo complessivo dell'appalto, non a un valore post-ribasso. Verificato con test dedicato: calcolo `calcolaEco()` con ribasso 10%; totali live e salvati coerenti; copertina coerente; nessuna regressione quando ECO = 0.
- **Correzione (14-7-2026, segnalata da Mauro): un documento di riferimento spariva dall'elenco "Documenti di riferimento" di un nuovo snapshot non appena la voce che lo citava veniva ricollegata a un documento diverso in un evento successivo** (es.: pezza3 citata da e.10.1 in PFTE; in PE si ritocca e.10.1 — anche solo per cambiarne lo stato — assegnandole pezza4: pezza3 scompariva dall'elenco del nuovo snapshot, e pezza4 ne prendeva il posto come "3" invece di comparire come "4"). **Non era un difetto del codice ma la conseguenza esatta e coerente del principio "ultimo evento vince" già validato per il Doc. Rif. (F6)** — verificato con test dedicato che gli snapshot già chiusi restavano comunque corretti, invariati, non intaccati da eventi successivi: il problema riguardava solo la costruzione dell'elenco nei *nuovi* snapshot. Chiesto esplicitamente a Mauro se l'elenco dovesse restare "solo documenti attualmente citati" o diventare cumulativo: **scelta la seconda** (coerente con la stratificazione già applicata il 13-7-2026 al menù a tendina delle pezze). L'elenco "Documenti di riferimento" di ogni snapshot ora comprende **tutti** i documenti mai usati come Doc. Rif. in un evento fino a quello snapshot (`ev.id<=ultimo_evento`), in ordine cronologico, indipendentemente dal fatto che siano ancora il riferimento corrente di qualche voce. Il **numero mostrato accanto a ogni voce** in tabella resta invece quello del documento attualmente di riferimento (F6 invariato) — semplicemente la sua posizione nell'elenco cumulativo può non essere più l'ultima. Verificato con test dedicato: l'elenco cresce (non sostituisce) quando una voce viene ricollegata a un nuovo documento; il numero della voce riflette sempre il documento corrente; gli snapshot storici restano invariati dopo eventi successivi.
- **Correzione (14-7-2026, segnalata da Mauro): una stima forfettaria imputata su una voce prima che raggiungesse la soglia di disaggregazione (es. e.8 valorizzata in TRI) spariva silenziosamente non appena la fase avanzava oltre la soglia (es. PFTE), mostrando 0,00 € finché nessuna sotto-voce veniva compilata.** Causa: `proiezione()` sovrascriveva incondizionatamente il valore della voce madre con la somma delle sotto-voci (tutte a zero, se mai toccate) non appena `disaggAttiva()` diventava vera. Corretto: la sovrascrittura scatta solo se **almeno una sotto-voce è stata effettivamente imputata** in un evento (nuovo tracciamento `toccata[]` in `proiezione()`); finché nessuna lo è stata, la voce madre mantiene l'ultimo valore imputato su di lei. Nuovo campo `sommata` nell'oggetto restituito da `proiezione()`, usato da `totali()`, `valida()` (R-01/R-02/R-08/R-09, voci negative) e `aggiornaTotali()` al posto del solo controllo di fase, per escludere la voce madre dai totali solo quando è davvero sostituita dalle sotto-voci (evitando sia il doppio conteggio sia la perdita silenziosa della stima). In tabella movimenti e in copertina, l'etichetta accanto alla voce madre distingue ora "(stima forfettaria, sotto-voci non ancora dettagliate)" da "(totale sotto-voci)" a seconda del caso. Il totale "in tempo reale" a fondo tabella e la cella della voce madre durante la compilazione di un evento seguono la stessa logica: mostrano la stima forfettaria finché non si digita in almeno una sotto-voce, poi passano alla somma di quanto digitato. Verificato con test dedicato: stima forfettaria preservata dopo il cambio di fase; totali corretti in entrambi gli stati; passaggio corretto da stima a somma non appena si digita la prima sotto-voce; nessuna regressione su voci non disaggregate, su ECO, su R-08.
- **Correzione (14-7-2026, segnalata da Mauro): in copertina, le colonne "Doc. Rif." e "Stato" erano troppo larghe e "Importo" troppo stretta, rompendo l'allineamento a destra delle cifre con importi lunghi.** Rimodulate le larghezze in `stampaCopertina()`: voce 8%→7%, importo 16%→26%, Doc. Rif. 22%→6% (ora contiene solo un numero), Stato 22%→15%.
- **Correzione (14-7-2026, segnalata da Mauro): l'elenco "Fonti documentali" in fondo al riquadro evento mostrava solo le pezze giustificative aggiunte nell'evento in corso, numerate da 1, disallineate rispetto al menù a tendina Doc. Rif. (che le mostra invece stratificate, es. a partire da "7." se 6 pezze storiche esistono già).** Corretto `renderListaFonti()`: mostra ora anche le pezze storiche (di eventi precedenti), in sola lettura senza pulsante "rimuovi" e con l'annotazione "(evento precedente)", nello stesso elenco numerato (`<ol>`) delle pezze nuove — numerazione quindi sempre unica e coerente con quella del menù a tendina.
- **Icona informativa "(i)" accanto a ogni voce, in tabella movimenti**, con tooltip al passaggio del mouse: "Per approfondimenti vai al § X.Y.Z del testo: «M. Moroni, C. Mazza - Il QE dell'opera pubblica»". Costruita una mappa `PARAGRAFO_LIBRO` (voce → paragrafo) leggendo per intero il capitolo 3 di "QE - il libro.pdf" e incrociandolo con le ~74 righe del dizionario voci. Dove il libro tratta più sotto-voci dell'app in un unico paragrafo comune (senza ulteriore articolazione nel testo), su indicazione di Mauro si è semplificato rimandando tutte al paragrafo padre: **tutte le e.8.\* → §3.2.6**, **tutte le e.9.\* → §3.2.7** tranne **e.9.12 → §3.2.8** (Oneri di assicurazione obbligatoria del personale, § che tratta esplicitamente l'assicurazione del personale interno). Le restanti ~55 voci hanno un riferimento puntuale e univoco (es. e.15 → §3.2.14, e.18.7 → §3.2.17.7). Icona presente solo in tabella movimenti (Registro eventi), non in copertina/stampa — per scelta esplicita di Mauro. Verificato con test dedicato: tutte le voci del dizionario hanno un riferimento in `PARAGRAFO_LIBRO`; le regole e.8.\*/e.9.\* rispettate; l'icona compare nell'HTML della tabella con il testo del tooltip corretto.

## v0.09 — 13-7-2026 — **congelata il 13-7-2026, non si tocca più** (`EasyQE_v0.09.html`, archiviata in `versioni/`)

- **Pezze giustificative dell'evento come elenco numerato "vivo"**, sullo stesso modello delle fonti di finanziamento e delle nomine: il box "Fonti documentali" non è più una serie di righe di campi vuoti da compilare tutte insieme, ma un mini-form fisso (Tipo doc, Codice, Titolo, Pag., Rev., Data) più un pulsante "+ Aggiungi pezza giustificativa" che, alla pressione, aggiunge subito la pezza a un elenco numerato visibile (con possibilità di rimuoverla) e pulisce i campi per la prossima. Introdotta la variabile di stato `fontiEvento[]`; `raccogliBozza()`/ripristino bozza aggiornati di conseguenza.
- **Nuova colonna "Doc. Rif." in tabella movimenti**, tra "Delta" e "Stato": un menù a tendina per ogni voce imputabile, con le opzioni tratte dalle pezze giustificative aggiunte all'evento in corso (elenco numerato: "1. …", "2. …"). Le voci calcolate (disaggregate, sola lettura) non hanno la colonna valorizzata.
- **Documento di riferimento obbligatorio per ogni voce con un valore imputato**: se non si seleziona un Doc. Rif. per una voce valorizzata, `registraEvento()` blocca con un messaggio dedicato. Questo controllo, più granulare, **sostituisce il vecchio R-17** ("ogni movimento richiede almeno una fonte documentale", verificato a livello di intero evento): se ogni voce con valore deve avere un documento specifico, la condizione precedente è automaticamente implicata — non serve più verificarla a parte.
- Il riferimento scelto (`doc_rif`, indice nell'array `fonte` dell'evento) viaggia con ciascun delta generato per la voce in quell'evento.
- **`proiezione()` traccia ora `docRif[voce]`**: il documento associato all'ultimo evento che ha imputato quella voce, coerente col principio già validato "un solo valore corrente = l'ultimo imputato" (doc 04, punto 1) esteso al documento di riferimento.
- **La colonna Doc. Rif. compare anche nello snapshot di stampa** (copertina), nella tabella "Quadro economico", tra l'importo e lo stato di ciascuna voce.
- Verificato con test dedicato: elenco pezze si popola e si svuota correttamente; select Doc. Rif. propone le pezze aggiunte; registrazione bloccata senza Doc. Rif. selezionato e passata con selezione; delta salvato con `doc_rif` corretto; `proiezione()` risolve `docRif` correttamente; la copertina mostra il documento di riferimento nella riga della voce.
- **Rifinitura (13-7-2026): nella copertina la colonna Doc. Rif. ora mostra solo un numero, non più il testo completo della pezza giustificativa.** In `stampaCopertina()`, prima di generare le righe, si costruisce un elenco deduplicato dei documenti effettivamente riferiti dalle voci mostrate (nell'ordine Quadro A, poi B, poi ECO): due voci che puntano allo stesso documento (stesso contenuto: tipo, codice, titolo, pagina, revisione, data) ricevono lo stesso numero. La cella Doc. Rif. di ogni voce mostra solo quel numero. Subito **dopo la riga ECO è stata aggiunta una nuova sezione "Documenti di riferimento"**, che riporta l'elenco completo (stesso testo prodotto da `fontiTxtSingola()`) numerato in corrispondenza. Se nessuna voce ha un Doc. Rif. risolto, la sezione mostra "Nessun documento di riferimento associato". Verificato con test dedicato: due voci con documenti diversi ricevono numeri distinti nell'ordine di apparizione; quando l'ultimo evento che imputa due voci diverse fa riferimento allo stesso documento, l'elenco deduplica correttamente e le due voci mostrano lo stesso numero.
- **Correzione concettuale (13-7-2026): l'elenco delle pezze giustificative proposto nel menù Doc. Rif. era stato progettato per azzerarsi a ogni evento — sbagliato, segnalato da Mauro.** Era possibile scegliere solo tra le pezze aggiunte nell'evento in corso, mentre serve poter riferire anche documenti già inseriti in eventi precedenti (l'elenco si "stratifica", non si azzera). Corretto: nuova funzione `pezzeStoriche()` raccoglie tutte le pezze già registrate in `db.registro_eventi` (una per ogni `ev.fonte[idx]`, con riferimento `"H:"+ev.id+":"+idx`); `opzioniDocRif()` propone ora l'elenco storico seguito dalle pezze nuove dell'evento in corso (`fontiEvento`, riferimento `"N:"+idx`), numerate in un'unica sequenza continua. `fontiEvento` continua a svuotarsi a ogni nuovo evento — correttamente: rappresenta solo le pezze *nuove* di quell'evento, non l'elenco completo, che ora è sempre disponibile via `pezzeStoriche()`. **Cambio di schema**: `delta[].doc_rif` può ora essere, oltre al numero di sempre (indice in `ev.fonte` dello stesso evento, usato per le pezze nuove), anche una stringa `"eventoId:idx"` quando il documento scelto proviene da un evento precedente; `proiezione()` risolve entrambi i formati. Piena retrocompatibilità: gli eventi già salvati con `doc_rif` numerico continuano a funzionare esattamente come prima. Verificato con test dedicato: il select propone correttamente sia le pezze storiche sia quelle nuove con numerazione unica progressiva; un evento che sceglie come Doc. Rif. una pezza di un evento precedente si registra correttamente e `proiezione()` risolve il documento giusto.
- **Rifinitura (13-7-2026): in copertina, sotto i titoli "QUADRO A" e "QUADRO B" è stata aggiunta una riga vuota di distanziamento e una riga di intestazione colonna** ("#" · Voce · Importo · Doc. Rif. · Stato, in grigio e corpo ridotto), per rendere leggibile a colpo d'occhio la tabella senza dover risalire alla legenda. Nessuna intestazione aggiunta sopra ECO (sezione a sé, una sola voce).
- **Bugfix (13-7-2026, segnalato da Mauro): il menù Doc. Rif. proponeva anche i documenti di nomina/cessazione dei soggetti, non solo le pezze giustificative del QE.** Causa: `pezzeStoriche()` raccoglieva `fonte` da *tutti* gli eventi del registro, ma gli eventi di sistema NOMINA e CESSAZIONE_INCARICO valorizzano quel campo con l'atto di incarico ("atto_incarico" + numero/data della delibera di nomina), un documento di natura completamente diversa dalle pezze giustificative del QE. Corretto: `pezzeStoriche()` raccoglie ora `fonte` solo dagli eventi con `tipo_innesco==="evento"` (quelli generati da `registraEvento()`, gli unici che possono legittimamente portare pezze giustificative), escludendo NOMINA, CESSAZIONE_INCARICO e CHIUSURA_FASE. Verificato con test dedicato: un registro con nomina + evento QE + cessazione produce un solo elemento in `pezzeStoriche()`, quello dell'evento QE; nessun riferimento ad "atto_incarico" nel menù risultante.

## v0.08 — 12-7-2026 — **congelata il 13-7-2026, non si tocca più** (`EasyQE_v0.08.html`, archiviata in `versioni/`)

- **Eliminata la qualifica di stato "necessario" da tutte le voci (richiesto espressamente da Mauro: "un fossile del passato — se una cifra è segnata in QE vuol dire che è necessaria").** `SERIE_MOV` passa da 4 a 3 serie selezionabili per i nuovi movimenti: `stanziato`, `impegnato`, `liquidato`. Aggiornati di conseguenza: il testo di aiuto sopra la tabella movimenti, il messaggio di errore "nessuna serie spuntata", l'export CSV della proiezione (colonna "necessita" rimossa da intestazione e righe).
- **Retrocompatibilità in lettura, non solo in scrittura**: `proiezione()` continua a riconoscere `serie:"necessita"` nei movimenti di eventi già registrati in file salvati con versioni precedenti, e la mappa delle etichette in `stampaCopertina()` continua a tradurla in "necessario" per gli snapshot storici. La rimozione riguarda solo la possibilità di *scegliere* quella serie per i movimenti nuovi, non la capacità di leggere correttamente ciò che è già stato salvato.
- **Correzione (12-7-2026, segnalata da Mauro subito dopo il primo rilascio della v0.08): la prima implementazione lasciava per errore l'obbligo di spuntare almeno una serie quando si imputa un valore** — bloccando la registrazione dell'evento con l'alert "nessuna serie spuntata". Non era quanto richiesto: il mandato era rendere le tre serie non obbligatorie, non solo eliminarne una quarta. Rimosso il blocco in `registraEvento()`: se non si spunta nessuna serie, il movimento si registra comunque (confrontato col valore corrente complessivo della voce anziché con una singola colonna di stato), richiede comunque una fonte documentale (R-17) e non alimenta nessuna delle tre colonne di stato in tabella/copertina/export — il valore corrente della voce resta corretto. `proiezione()` distingue ora esplicitamente `serie:"stanziamento"` (colonna stanziato) da `serie:null`/assente (nessuno stato), che prima sarebbe caduta per errore nella stessa colonna "stanziato".
- Verificato con test dedicato: `SERIE_MOV` non contiene più "necessita"; un evento storico con quella serie viene comunque sommato correttamente in proiezione; nessuna checkbox "necessario" compare più in tabella movimenti; l'header CSV non ha più quella colonna; un movimento senza nessuna serie spuntata non blocca più la registrazione e non finisce per errore nella colonna "stanziato".
- Versione visualizzata in titolo e intestazione aggiornata a v0.08.

## v0.07 — 12-7-2026 — **congelata il 12-7-2026, non si tocca più** (`EasyQE_v0.07.html`, archiviata in `versioni/`)

- **Granularità fase-dipendente delle voci** (nuovo meccanismo generale, applicabile ad altre voci in futuro): una voce madre può portare l'attributo `disaggregazione:{da_fase, sotto_voci:[...]}` nel dizionario voci. Prima della fase soglia è imputabile normalmente come oggi; da quella fase in poi diventa un **totale calcolato** (sola lettura, somma delle sotto-voci correnti) e le sotto-voci diventano editabili indipendentemente. La transizione è agganciata solo al cambio di fase (nessun'altra condizione), è irreversibile, e non ripartisce il valore storico pregresso: il valore imputato prima della soglia resta congelato negli snapshot di quelle fasi; le sotto-voci partono libere, ciascuna dal proprio zero.
- **Voci coinvolte in questa versione** (tutte con soglia F.03 — PFTE): `e.8a` (10 sotto-voci, funzioni esterne), `e.8b` (10 sotto-voci, funzioni interne — stessa articolazione di e.8a salvo la prima voce, diversa per natura: supporto al RUP vs attività DEC), `e.10` (4 sotto-voci), `e.14` (3 sotto-voci: collaudo t.-a., statico, specialistici), `e.16` (5 sotto-voci: accordi bonari, transazioni, CCT, precontenzioso ANAC, lodi arbitrali — ciascuna eredita individualmente il regime `ad_attivazione`/`potenziale` già presente sulla voce madre), `e.18` (7 sotto-voci: IVA per aliquota 4/10/22%, rivalse casse ingegneria/architettura, forense, altri professionisti, contributo ANAC). 39 nuove sotto-voci in totale; dizionario voci passato da 25 a 64 elementi.
- **e.18 resta a compilazione manuale**: le sue sotto-voci non calcolano automaticamente IVA per aliquota, rivalse o contributo ANAC — sono caselle libere come le altre. Il calcolo automatico (R-04, rivalse, R-12/ANAC) resta roadmap separata, non toccata in questa versione.
- **Motore reso fase-aware** per sostenere il meccanismo: `proiezione()` accetta ora un secondo parametro opzionale (fase di riferimento) e, per ogni voce disaggregata attiva, sovrascrive il suo valore corrente con la somma delle sotto-voci; `totali()`, `valida()`, `aggiornaTotali()`, `stampaCopertina()` e `csvProiezione()` escludono le voci-madre calcolate dalle somme dirette per evitare il doppio conteggio. Verificato con test mirato: **R-08 e R-09 continuano a leggere correttamente il totale calcolato** di e.8b/e.10 dopo la soglia, senza modifiche al testo delle regole.
- UI: le sotto-voci non ancora attive (fase precedente alla soglia) non compaiono in tabella; le voci-madre disaggregate compaiono come riga di sola lettura con etichetta «totale delle sotto-voci», il valore si aggiorna in tempo reale mentre si compilano le sotto-voci sottostanti.
- Aggiunto `bozza_evento:null` esplicito alla creazione di un nuovo file e alla lettura di file precedenti privi del campo (retrocompatibilità v0.06 → v0.07).
- **Bugfix collaudo (12-7-2026)**: le sotto-voci, nella tabella movimenti e nella stampa dello snapshot, sono ora **indentate** rispetto alla voce madre a cui fanno capo (rientro sulla colonna descrizione), per rendere leggibile a colpo d'occhio la gerarchia voce/sotto-voce. *(Segnalato un falso allarme durante il collaudo: aprendo con la v0.07 un file `.json` creato con la v0.06, il dizionario voci resta quello vecchio — senza le sotto-voci — perché viene letto interamente dal file, non ricalcolato dall'app. Comportamento atteso, non un bug: creando un'opera nuova con "Nuova opera" l'articolazione funziona correttamente. La migrazione del dizionario voci per i file esistenti resta un punto aperto, non ancora richiesto.)*
- **Decisione di prodotto (12-7-2026): il controllo di copertura finanziaria NON è una funzione della prima release operativa di EasyQE.** Rimossi: la regola R-05 (QE coperto ≤ fonti), il calcolo del "QE coperto" in `totali()`, gli attributi `regime_copertura`/`stato` dal dizionario voci (comprese e.16 e le sue 5 sotto-voci, che perdono la distinzione potenziale/attiva), il campo `copertura` sugli eventi di sistema, la colonna "QE coperto" nella Raccolta snapshot e nei dati salvati di ogni snapshot, la colonna `copertura` nell'export CSV del registro eventi. **Restano invariate**, per scelta esplicita: le fonti di finanziamento (tab Testata, form ed elenco) e il dato "Finanziamenti totali disponibili" in copertina — restano come informazione anagrafica su chi finanzia l'opera, senza alcun controllo o vincolo collegato. Verificato con test dedicato che, con fonti dichiarate molto inferiori al QE, nessun blocco scatta più; verificato che la disaggregazione fase-dipendente di e.16 continua a funzionare correttamente senza i due attributi rimossi. Non è un arretramento di roadmap come le altre regole non ancora implementate (R-04, R-06, ecc.): è una rimozione deliberata, da riconsiderare in una release successiva se necessario.
- **ECO esclusa dai totali di quadro B e dal QE complessivo (12-7-2026).** La voce ECO (economie da ribasso) aveva natura `additiva` come le altre voci del quadro B e veniva quindi sommata al Totale quadro B e al Totale complessivo — errore concettuale: un'economia da ribasso è un risparmio accantonato, non un costo aggiuntivo, e sommarla gonfiava il QE totale. Corretto in `totali()`, `valida()` (R-02, che calcola il costo complessivo), `aggiornaTotali()` e `stampaCopertina()`: ECO non contribuisce più a nessun totale. In tabella movimenti e in copertina, ECO è stata spostata fuori dal blocco QUADRO B e compare come riga a sé **dopo il TOTALE COMPLESSIVO**, con l'etichetta "accantonate, non incluse nel totale QE/complessivo"; resta imputabile con lo stesso meccanismo di oggi (percentuale di ribasso sull'importo ribassabile, o valore diretto). L'export CSV della proiezione non richiedeva modifiche (elenca le voci senza calcolare un totale). **Non tocca** il percorso a stati (costituzione → vincolo → utilizzo → residuo, R-06) né il collegamento con gli eventi che consumeranno le economie nelle fasi successive: resta un intervento distinto, non ancora implementato. Verificato con test dedicato: ECO esclusa da `totali().tot`/R-02/quadro B ovunque; riga ECO renderizzata dopo il marcatore "TOTALE COMPLESSIVO" sia in tabella movimenti sia in copertina.
- **Bugfix (12-7-2026, trovato in collaudo dopo A1-A10 tutti OK): il CIG principale in testata non veniva mai salvato.** `leggiTestata()` leggeva CUI e CUP dal form ma non il campo CIG (`tCig`): il valore digitato restava visibile a schermo ma non veniva mai scritto in `db.testata.cig_principale`, che restava `null`. Effetto: R-14 (CIG obbligatorio prima della revisione post-gara) bloccava sempre l'evento `REVISIONE_POST_GARA`, anche con il CIG apparentemente inserito. Aggiunta la riga mancante in `leggiTestata()`. Verificato con test dedicato: il CIG digitato viene ora salvato (trim incluso) e R-14 non blocca più quando il campo è valorizzato.
- **Fusione e.8b + e.9 → e.9; rinomina e.8a → e.8 (12-7-2026).** Verificato sulla fonte normativa (art. 5 Allegato I.7, comma 1 lett. e, nn. 8 e 9) e dottrinale ("QE - il libro", cap. 3.2.7) che le due voci NON erano interamente sovrapposte come inizialmente ipotizzato: solo la componente "verifica preventiva della progettazione" era duplicata (già sotto-voce e.8b.6 e insieme contenuto proprio della e.9 originaria); le altre due componenti della e.9 originaria — supporto al RUP se personale dipendente, assicurazione dei progettisti dipendenti — non avevano équivalente in e.8b. Concordata con Mauro la fusione strutturale: **e.8** (ex e.8a, invariata: 10 sotto-voci, funzioni esterne) e **e.9** (fusione di ex e.8b + e.9 originaria: 12 sotto-voci — le 10 di ex e.8b più "Supporto al RUP (personale dipendente)" ed "Assicurazione dei progettisti dipendenti", eliminando la duplicazione sulla verifica). Non esiste più `e.8a`/`e.8b` nel dizionario. **R-08** aggiornata da "e.8b + e.9" a un solo addendo "e.9" (stesso perimetro economico di prima, ora su un'unica voce). Verificato con test dedicato: nessun riferimento residuo a e.8a/e.8b nel codice; e.8 ed e.9 si disaggregano correttamente da F.03 con i contenuti attesi; R-08 scatta/non scatta correttamente sopra/sotto soglia leggendo solo e.9.

## v0.06 — 11-7-2026 — **EasyQE, RELEASE CONSOLIDATA** (`EasyQE_v0.06.html`, congelata, archiviata in `versioni/`)

- Identica alla v0.05 nel contenuto; consolida il nuovo nome **EasyQE** anche nel nome file. È la versione per la demo del 12-7. **Collaudo completo: test 1–24 tutti passati** (11-7-2026).

## v0.05 — 11-7-2026 (congelata l'11-7-2026, archiviata in `versioni/`)

- Tutti i campi data con calendario a discesa (input date) e visualizzazione in formato italiano gg/mm/aaaa; cessazione soggetto con data da calendario inline (niente più prompt).
- Nuovo campo obbligatorio **Descrizione dell'evento** (max 100 caratteri): è il nome dell'evento nella Raccolta snapshot (colonna «Evento», dopo la fase con codice e descrizione completa) e nella copertina.
- Totali ridefiniti: soggette a ribasso = **a + d**; non soggette = **b + c**; nuova riga **«Importo totale dell'appalto (a+b+c+d)»** sotto i due totali, in calce ad A e a B; base delle economie (% su ECO) aggiornata ad a + d.
- Copertina snapshot: eliminata la riga Atto; «Livello» → **«Evento»** (contenuto = descrizione dell'evento); «Data snapshot» → **«Data dell'evento»**.
- **Bugfix**: il «QE totale» degli snapshot risultava zero — leggeva un registro interno anziché il valore corrente delle voci; ora coincide col Totale complessivo della tabella voci.
- Calce del quadro B ridotta a tre righe: Totale quadro B · Importo totale dell'appalto · Totale complessivo (i totali a+d e b+c restano solo in calce al quadro A).
- **Persistenza degli stati**: le caselle necessario/stanziato/impegnato/liquidato di ogni voce si ripresentano nel nuovo evento **già evidenziate** come nell'ultimo evento che ha targato la voce (anche in combinazione); l'evento salva gli stati dichiarati (`stati`), e per gli eventi precedenti alla modifica gli stati sono ricostruiti dai movimenti.
- **App rinominata: EasyQE** (titolo pagina e intestazione).
- Snapshot PDF, affinamenti: font Arial (titolo 14 grassetto, denominazione 14, resto 10, titoli sezione/tabella 10 grassetto su fondo #f3f3f3); testi di descrizione e presentazione giustificati; eliminata la riga «QE a copertura obbligatoria»; «Fonti di finanziamento» → **«Finanziamenti totali disponibili»** con l'elenco delle fonti del registro sotto il totale; «QUADRO B — Somme a disposizione dell'Amministrazione» con riga vuota sopra; eliminato il footer interno con hash. *(Il pathname nel piè di pagina lo aggiunge il browser: disattivare «Intestazioni e piè di pagina» nella finestra di stampa.)*
- Totali degli snapshot ricostruiti dal registro alla data dello snapshot (corregge lo zero congelato nei snapshot creati col bug).
- **Snapshot stampato riorganizzato**: titolo · **Descrizione dell'opera** · **Presentazione e inquadramento del QE** · **Sintesi e macro-dati** (tabella CUI→Fonti) · **Identificazione figure chiave** · salto pagina · **Quadro economico** completo, diviso in quadro A e quadro B con l'articolazione della tabella voci, gli stati evidenziati per voce e i totali (a+d, b+c, importo totale appalto, totale B, totale complessivo). Gli importi e gli stati sono ricostruiti alla data dello snapshot (fino al suo ultimo evento). Le proiezioni congelate dei nuovi snapshot usano il valore corrente delle voci.

- **Bugfix R-19 (falso positivo, trovato dal collaudo di Mauro, test post-16)**: gli eventi NOMINA/RETTIFICA conservavano un *riferimento* alla scheda soggetto anziché una copia; la successiva cessazione (o modifica) del soggetto alterava retroattivamente il contenuto dell'evento già registrato, rompendo la catena hash alla riapertura del file («hash non valido all'evento #N»). Ora l'evento incorpora una **copia immutabile** della scheda al momento della nomina. I file salvati con la v0.04 che contengono nomine poi cessate manterranno l'avviso all'apertura: i dati sono leggibili ma la catena è marcata; per i collaudi ricreare il file.

Regole di versioning (adottate il 9-7-2026):

1. Ogni versione dell'app è un file distinto: `QE_app_vX.YY.html`. **Nessun file di versione precedente viene mai sovrascritto.**
2. Ogni copia congelata è archiviata in `versioni/`.
3. Ogni nuova versione ha una voce in questo changelog con le modifiche introdotte.
4. Il file di lavoro corrente è sempre quello con il numero più alto.

---

## v0.01 — 8/9-7-2026 (congelata il 9-7-2026, archiviata in `versioni/`)

Prima versione funzionante, sviluppata e collaudata iterativamente con Mauro. Contiene:

- Testata (dati sintetici salienti): nickname (max 10, nome file `QE_<CUP|CUP_DA_DEFINIRE>_<nickname>.json`), option box soglia UE, modalità di affidamento, contratto a misura/corpo con motivazione, L. 717 con motivazione, fase corrente in sola lettura.
- Fonti di finanziamento con tipologia/fonte a cascata (catalogo da `esempio fonti.txt`) e vincoli.
- Soggetti: anagrafe a validità temporale, tipologie condizionate dal ruolo (persona singola/società/RTI/commissione), CCT sempre commissione di 3, referente società e responsabile redazione QE per RTI, modifica scheda con evento RETTIFICA, atto-tipo a menù.
- Fasi schema 1.2: sequenze ordinario/integrato (TRI→ANN→PFTE→PE/AGG→AGG/PE→ESEC_SAL→ESEC_SF→COLL→CHIU), eventi condizionati per fase, stati QE derivati (previsionale/assestato/rimodulato/finale).
- Registro eventi append-only con catena hash SHA-256; responsabile di fase a option box (RUP/DEC/Progettista) con nominativo autocompilato; tabella voci con versione precedente / versione attuale / delta calcolato (rosso/verde) e serie a caselle multiple (necessario/stanziato/impegnato/liquidato); fonti documentali obbligatorie (R-17); bozza evento (salva e continua dopo / salva e concludi).
- Snapshot a due livelli: per evento (automatico) e per chiusura di fase (pulsante «Chiudi fase», con avanzamento di fase e validazioni bloccanti); copertina statica stampabile con placeholder ruoli.
- Export CSV (registro e proiezione con riferimenti documentali) e salvataggio JSON.
- Formato numerico italiano ovunque; palette rosso E1231B; layout 1600px.

## v0.02 — 10-7-2026 (congelata, archiviata in `versioni/`)

- Identica alla v0.01 nel contenuto; apre la linea di versioning disciplinato (header aggiornato). *Nota: una prima lavorazione erroneamente scritta su questo file è stata spostata nella v0.03 — la regola «nessuna versione si sovrascrive» è stata violata una volta e il ripristino è documentato qui.*

## v0.04 — 11-7-2026 — **RELEASE STABILE DI PARTENZA** (congelata, archiviata in `versioni/`; le verifiche si svolgono su questa versione con dati nuovi; i dati di prova precedenti sono da considerarsi privi di valore)

- Tabella voci dell'evento: quattro righe di totali **in calce ai quadri** con precedente/attuale/delta in tempo reale, nell'ordine: Totale quadro B · Totale soggetto a ribasso (quadro A) · Totale non soggetto a ribasso (quadro A) · Totale complessivo. I totali considerano il valore imputato dove presente, altrimenti il valore corrente della voce.

## v0.03 — 10-7-2026 (congelata l'11-7-2026, archiviata in `versioni/`)

- Rimosso il campo CIG dall'evento: il CIG principale si imputa in **testata** quando disponibile (campo sbloccato); la Revisione post-gara verifica che sia presente in testata (R-14 riformulata).
- Rimosso il campo «Copertura» a livello di evento: la copertura è un attributo della singola voce, non dell'evento.
- Rimosso il campo «Ribasso %» dall'evento: la casella percentuale del ribasso di gara sta **accanto all'etichetta «Economie da ribasso»**; applicata all'importo ribassabile (voce a) calcola le economie e compila il valore attuale, che resta modificabile e da documentare con pezze giustificative.
- Descrizione voce ECO ripulita («posta segregata» eliminato); campo importo di ECO riportato alla larghezza standard della colonna.
- Tabella voci: celle «Versione precedente» e «Delta» bordate come il campo di input, tre colonne a larghezza uguale (150px), etichette con [€]; delta in nero su sfondo rosso chiaro (aumento) o verde chiaro (diminuzione); etichetta «Serie» → «Stato»; tolto il corsivo dalla riga «di cui» (MAN).
- **Bugfix**: la copertina degli snapshot di evento andava in errore (atto di approvazione assente) e il pulsante «Copertina/stampa» non produceva nulla; ora l'atto è opzionale, la copertina indica il livello (snapshot di evento n. X / chiusura fase) e la stampa funziona per entrambi i livelli.
- **Bugfix (valore corrente)**: validazioni e colonna «Versione precedente» operavano su registri interni per stato invece che sul valore della voce — in F.01 le regole giravano su zeri e la versione precedente non si popolava. Ora ogni evento salva il **valore totale imputato per voce** (`valori`): quello è il valore corrente del QE, fa da «Versione precedente» all'evento successivo, alimenta il delta e le regole di validazione (attive fin da F.01). Le quattro colonne di stato (necessario/stanziato/impegnato/liquidato) restano come qualificazione degli importi.
